{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/main/main.go",
            "args": [
                "-o",
                "output.log"
            ],
            "cwd": "${workspaceRoot}" // Укажите рабочую директорию
        }
    ]
}